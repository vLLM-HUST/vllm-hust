# OpenCV is optional for text-only inference. Keep the Ascend host pin compatible
# with NumPy 1.26 while retaining the existing requirement on other platforms.
mistral_common[image] >= 1.11.3
opencv-python-headless == 4.11.0.86; platform_machine == "aarch64"
opencv-python-headless >= 4.13.0; platform_machine != "aarch64"
